# 프로젝트 메모리 (AsangP Frontend)

## 2026-05-11 월요일
### 진행 상황
- SvelteKit 2.0 및 Svelte 5 (TypeScript) 프로젝트 초기화 완료
- Prettier, ESLint 추가 완료
- `MEMORY.md` 생성 및 초기 기록 시작
- 백엔드 API 연결 완료 (`https://api.eulhane.com/asangp`)
- `.env` 파일을 통한 API 베이스 URL 관리 설정 (서버 주소 반영)
- 의존성 패키지 설치 완료 (`npm install`)
- CORS 이슈 해결을 위한 Vite 프록시(Proxy) 설정 적용 (`/api` 경로)
- 메인 대시보드 구현 완료 (4개 공간 실시간 상태 표시 및 상세 페이지 연결)

## 2026-05-26 화요일
### 진행 상황
- 상세 페이지에 Chart.js 연동 및 7일 전 대비 혼잡도 추세 차트 구현
- Chart.js `time` 스케일 및 `date-fns` 어댑터 도입으로 시계열 줌/팬 오류 근본 해결
- X축 레이블 10분 단위 정밀 정렬 (`ticks.source: 'data'`)
- UX 개선: 혼잡도 숫자 숨김 처리 및 상태 텍스트(여유/보통/혼잡) 툴팁 적용
- 날짜 선택 기능 추가 (과거 데이터 조회 지원)
- 대시보드 레이아웃 개선: 상단 바 파란색 배경 적용 및 카드 레이아웃 유연화 (고정 비율 제거 및 가로 확장 지원)

## 2026-06-01 월요일
### 진행 상황
- 상세 페이지 하단 피크 시간대 표 3열에 24시간 추세 미니 차트(스파크라인) 개선 및 적용
- 스파크라인 차트의 X축/Y축, 툴팁, 범례 숨김 처리
- 스파크라인 내 줌(Zoom) 및 팬(Pan) 기능 명시적으로 비활성화 및 애니메이션 제거하여 성능 최적화
- 데이터 중 `null` 값 처리 오류를 수정하여 빈 데이터를 0으로 간주하도록 수정 (`val ?? 0` 적용)
- 스파크라인 차트의 선 굵기를 1px로 얇게 통일하고, 0일 때 캔버스 경계에 선이 잘려 얇아 보이는 시각적 이질감을 방지하기 위해 상하 여백(Padding) 추가
- 메인 페이지에 있던 전역 CSS(글꼴, 스크롤 방지)가 라우팅 시 다른 페이지에 영향을 주어 발생하던 스크롤 불가 및 폰트 미적용 버그 해결 (`app.css` 전역 분리 적용)
- 메인 페이지의 상단 로고 및 타이틀 헤더를 `Header.svelte` 컴포넌트로 분리하고 상세 페이지에도 동일하게 적용
- 전체 앱의 기본 배경색을 `#f5f7fa`로 통일하여 일관성 개선
- 상세 페이지의 불필요한 기존 헤더 블록 삭제 및 PC 화면(`1024px` 이상)에서 스크롤 없이(One-page) 한눈에 볼 수 있도록 차트와 표를 좌우 분할 그리드 레이아웃으로 개편
- 우측 피크 시간대 표의 날짜 형식을 간결하게 변경('2026-05-25' -> '5월 25일')하고 공간 확보를 위해 테이블 헤더(th) 행 삭제
- 상세 페이지 그래프 범례(Legend) 숨김 처리하여 불필요한 텍스트 제거
- 차트 내 대상일(오늘) 혼잡도 선 색상을 헤더와 동일한 짙은 남색(`#072e5d`)으로 변경하고, 7일 전 비교선은 회색 실선으로 변경 및 두 선의 굵기를 1px로 얇게 조정하여 디자인 통일감 부여
- 화면 크기 축소 시 차트와 표가 그리드 밖으로 잘리는 현상(Overflow)을 방지하기 위해 각 섹션에 `min-width: 0` 속성을 추가하여 반응형 레이아웃 정상화
- 모바일(1024px 미만) 환경에서는 차트와 표가 세로로 배치되는 점을 고려해 화면 높이 고정(`100vh`)을 해제하고 자연스러운 네이티브 세로 스크롤이 가능하도록 모바일 최적화 수정
- 상세 페이지의 차트 및 표 컨테이너 디자인을 매트(Matte)하고 깔끔한 무드로 변경(반투명 블러 효과, 그림자, 테두리 제거 후 단색 흰색 배경 적용)
- 우측 피크 시간대 표의 스크롤바를 완전히 제거하고, 표 크기(`height: 100%`)가 자동으로 부모 컨테이너 높이에 딱 맞게 늘어나 한눈에 전체 데이터가 꽉 차게 보이도록 수정
- 상세 페이지 그래프의 Y축 최대값이 데이터에 따라 변동되지 않고 항상 100%로 고정되도록 수정하여 일관된 혼잡도 확인 기능 향상
- 모바일 화면(768px 이하)에서 `Header` 컴포넌트의 학교 로고와 제목이 겹쳐 보이는 현상을 방지하기 위해, 텍스트는 화면 중앙에 두고 로고 컨테이너는 좌측에 고정하되, 로고 이미지 아래에 '아주대학교' 텍스트가 위치하도록 세로로 배치(`flex-direction: column`)하여 가로폭을 줄임으로써 겹침 현상을 해결함
- 상세 화면 차트 하단에 표시되던 데이터 보정 관련 안내 텍스트("* 데이터가 없는 시간대는...")를 삭제하여 화면을 더 깔끔하게 구성함
- 차트 X축의 최대 범위를 다음날 05:50에서 06:00으로 연장하고 눈금(`ticks`) 생성 방식을 데이터 기준(`source: 'data'`)에서 자동(기본값)으로 변경하여, 모바일 환경 등에서 우측 끝 06:00 눈금이 잘리지 않고 정상적으로 표시되도록 수정함
- 상세 화면 상단의 차트 사용 안내 문구를 "차트를 드래그하거나, 확대/축소하여 원하는 시간대를 상세히 확인해 보세요."로 부드럽고 자연스럽게 변경함
- 기존의 딱딱한 날짜 입력창(`input type="date"`) 대신 달력 아이콘 이미지와 텍스트(예: "6월 4일 (오늘)")로 구성된 뱃지 형태의 커스텀 UI를 적용하고, 양옆에 이전/다음 날짜로 이동할 수 있는 좌우 화살표 버튼을 추가함. 미래 날짜나 제한 일자 이전으로는 이동할 수 없도록 화살표를 회색으로 비활성화하는 로직도 함께 적용함
- 날짜 뱃지 및 좌우 화살표 버튼의 크기가 모바일 등 화면 크기에 맞춰 자연스럽게 축소/확대되도록 CSS `clamp()` 함수를 사용해 반응형 사이즈 조절을 적용함
- 날짜 선택기의 최소 제한 일자를 임의의 날짜(5월 11일)에서 오늘 날짜(당일)로 변경하여, 과거 날짜(어제 등)로 이동할 수 없도록 이전 화살표를 비활성화함
- 하단의 '과거 7일 피크 시간대' 표 영역에 있는 기준 날짜 선택기에도 메인 차트와 완전히 동일한 뱃지 스타일 UI와 화살표 이동 로직을 통일하여 적용함
- 피크 시간대 데이터는 항상 어제(전 날) 데이터를 기준으로 보여주므로, 하단 달력의 기준 날짜 제한 로직을 오늘이 아닌 '어제 날짜'로 분리하여 적용함.
- 달력 뱃지 텍스트에 나타나던 '(오늘)', '(어제)' 같은 문구를 모두 삭제하고, 날짜에 상관없이 일관되게 요일(예: `6월 4일 (목)`)만 표시되도록 포맷팅 로직을 수정함.
- 하단 피크 시간대 표에 데이터가 없을 때 표시되던 '데이터를 불러오거나 백엔드 API가 준비되지 않았습니다.'라는 시스템 에러성 문구를 사용자가 직관적으로 이해하기 쉽도록 '해당 날짜의 데이터가 없습니다.'로 간결하게 수정함.
- 모바일 브라우저(특히 iOS Safari 등)의 네이티브 달력 위젯이 `<input type="date">`의 `min`, `max` 속성을 무시하고 비활성화된 날짜를 선택할 수 있게 허용하는 브라우저 스펙/버그 이슈를 해결하기 위해, Svelte 컴포넌트 내부(JavaScript)에 날짜 유효성 검사 로직을 추가하여 제한된 날짜를 선택하면 허용된 최소/최대 날짜로 강제 보정되도록 방어 코드를 구현함
- 모바일 환경에서도 선택 불가능한 날짜(미래 및 제한 일자 이전)가 시각적으로 완벽히 회색(비활성화) 처리되어 터치조차 할 수 없게 만들고자, 네이티브 달력 위젯을 띄우는 방식을 버리고 커스텀 달력 라이브러리인 `flatpickr` 패키지(`npm install flatpickr`)를 도입하여 PC와 모바일 모두 동일하고 세련된 달력 UI가 나타나도록 고도화함
- 아이폰(iOS Safari) 등 모바일 브라우저에서 날짜 뱃지(`input` 태그)를 터치했을 때 글자 크기가 16px 미만이면 브라우저가 강제로 화면을 줌인(확대)하는 기본 동작을 방지하기 위해, 투명하게 숨겨진 입력창(`.hidden-date-input`)의 폰트 사이즈를 강제로 16px로 고정하여 사이트 확대 현상을 해결함
- 헤더 영역의 '자리찾아주' 제목과 서브타이틀의 글씨 크기를 모바일 화면에서만 확대하여 시인성을 개선함 (PC 화면은 너무 커 보인다는 피드백을 반영하여 원래의 크기로 원복)
- 메인 차트 좌측 상단의 '공간 이름 혼잡도 분석' 텍스트(`<h2>`)와 우측의 퍼센트 뱃지(`span.status-tag`)가 시각적으로 Y축 중앙 정렬이 어긋나 보이던 현상을 해결함. 텍스트(`<h2>`)의 `line-height`를 1로 고정하여 외부 정렬을 맞추고, 뱃지 내부 텍스트 역시 약간 위로 치우쳐 보이던 현상을 `line-height: 1`과 상하 패딩(`padding: 0.35rem 0.8rem`) 조절을 통해 기존의 통통한 뱃지 모양(높이)은 유지하면서 글자만 타원 정중앙에 위치하도록 완벽히 교정함
- PC 등 넓은 화면에서 차트나 피크 시간대 표에 데이터가 없을 경우 출력되는 '해당 날짜의 데이터가 없습니다.' 문구가 영역의 위쪽으로 치우쳐 보이던 현상을 수정함. Flexbox(`align-items: center`, `justify-content: center`)와 영역 높이(`height: 100%`, `min-height`)를 활용하여 어떤 화면 크기에서도 빈 공간의 정확히 상하좌우 정중앙에 문구가 예쁘게 뜨도록 레이아웃을 개선함
- 하단 피크 시간대 표에서 피크가 없을 때 노출되던 "혼잡 시간대 없음" 문구를 "대체로 한산한 편"으로 수정하여 훨씬 부드럽고 자연스러운 느낌을 주도록 개선함
- 표 안에 피크 시간대 뱃지가 3개 이상 노출될 때 뱃지가 줄바꿈되면서 동일 행의 날짜 텍스트(예: "5월 31일")까지 세로로 어색하게 줄바꿈되던 문제를 해결하기 위해, 날짜 영역 렌더링 시 `white-space: nowrap`을 강제로 적용하여 날짜 칸은 항상 한 줄을 깔끔하게 유지하도록 방어 코드 추가
- 피크 시간대가 여러 개 뜰 경우 기존의 핑크색 뱃지 디자인이 시각적으로 너무 튀거나 뭉쳐 보이던 현상을 개선하기 위해, 뱃지 형태를 걷어내고 작은 빨간 점(가상 요소 `::before` 활용)과 짙은 회색 기본 텍스트(`color: #334155`)로만 심플하게 표현하는 디자인으로 전면 교체하여 데이터 가독성과 대시보드다운 세련미를 높임
- 하단 피크 표에서 달력 날짜(예: 6월 3일)를 선택했을 때 백엔드 API가 선택된 당일을 포함하여 과거 7일을 반환하는 스펙(5/28~6/3) 때문에 사용자가 의도한 과거 7일(5/27~6/2)과 불일치하는 버그를 수정함. 프론트엔드(`+page.ts`)에서 API 요청 파라미터를 넘길 때 선택된 날짜에서 하루를 뺀 날짜(`target_date - 1일`)를 쿼리로 전송하도록 보정하여 데이터가 정확하게 매칭되도록 해결함
- 하단 표의 제목이었던 "과거 7일 피크 시간대" 문구를 대시보드 성격에 더 부합하고 세련된 느낌을 주는 "주간 혼잡도 추이"로 교체함
- 상단 메인 차트의 제목을 "{공간이름} 혼잡도 분석"에서 "{공간이름} 혼잡도 차트"로 변경하여 더 직관적이고 심플하게 수정함
- 모바일 화면(작은 폭)에서 하단 표의 우측 '미니 차트(스파크라인)'가 화면 밖으로 잘려서 보이지 않던 문제를 해결함. 표 전체의 강제 최소 너비(`min-width: 500px`) 제한을 풀고, 표 내부의 좌우 여백(Padding)과 미니 차트의 가로폭을 `clamp()` 함수를 이용해 모바일 환경에 맞춰 유연하게 줄어들도록 압축시켜 스크롤 없이도 모든 정보가 한 화면에 쏙 들어오게끔 반응형 레이아웃을 고도화함
- 모바일에서 표 영역 폭을 압축함에 따라 "대체로 한산한 편 (최고 n%)" 문구가 두 줄로 꺾이는 현상을 방지하기 위해, 표 바깥을 감싸고 있던 거대한 고정 여백(Padding 1.5rem)을 모바일에서는 대폭 줄여 표 안쪽이 텍스트를 온전히 표시할 수 있도록 실제 가용 가로폭을 대폭 확장함. 추가로 해당 문구 자체에 `white-space: nowrap`을 적용하여 어떠한 경우에도 글자가 꺾이지 않도록 레이아웃 안전성을 극대화함
- `new Date("YYYY-MM-DD")` 방식이 문자열을 UTC 기준으로 파싱하여, 로컬 타임존(예: UTC-5) 환경에서 실제 날짜보다 하루 적게 계산되어 날짜 이동 시 건너뛰거나 오작동하는 치명적인 버그를 해결함. `+page.svelte`와 `+page.ts` 전반에 걸쳐 날짜 문자열을 `-` 기준으로 분리(`split('-')`)한 뒤 `new Date(year, month - 1, day)`를 통해 로컬 타임존 기준으로 안전하게 Date 객체를 생성하도록 전면 수정함
- 서비스의 오프라인 공간 위치 기준을 맞추기 위해, 사용자가 해외(다른 타임존)에서 접속하더라도 현재 날짜(`todayStr`, `yesterdayStr`, `targetDate`) 계산이 항상 브라우저 로컬 시간이 아닌 **서버 위치(한국 시간 KST, Asia/Seoul)**를 기준으로 고정되도록 `toLocaleString`을 이용해 타임존을 강제 지정하는 로직으로 프론트엔드 기준 시간을 전면 재편함
- 상세 페이지 진입 시 하단 주간 혼잡도 표의 달력 기본값이 무조건 전 날(어제)로 세팅되던 것을, 상단 차트와 동일하게 당일(오늘)이 기본적으로 선택되도록 스펙을 변경하고 그에 맞춰 달력의 이동 제한 영역(`maxPeakDateStr`, `minPeakDateStr`)도 오늘을 기준으로 재조정함
- 서버 사이드 렌더링(+page.ts) 단에서 외부 API를 호출하기 위한 날짜(어제 날짜)를 계산할 때, 기존 로컬 기반 계산법 대신 아예 타임존의 영향을 원천 차단하기 위해 `Date.UTC`와 `getUTC*` 메서드를 도입하여 완벽한 '순수 UTC 기반 날짜 포맷팅' 구조로 업그레이드함
- 달력에서 과거 날짜로 이동하는 것을 제한하는 최솟값(`minDateStr`, `minPeakDateStr`)이 기존에는 계속 변하는 `todayStr`로 묶여 있어서, 내일이나 모레가 되어도 과거(서비스 개시일 포함) 데이터를 전혀 볼 수 없는 버그가 발생할 수 있음을 확인하고, 이를 하드코딩된 특정 '서비스 개시일(`SERVICE_LAUNCH_DATE = 2026-06-04`)' 상수로 고정하여 캘린더 이동 범위를 명확히 제한함
## 2026-06-04 목요일
### 진행 상황
- 상세 페이지(`+page.ts`) 진입 또는 날짜 변경 시 발생하는 상태, 이력, 피크 데이터 등 3개의 API 통신을 기존 순차적(Sequential) 방식에서 `Promise.all`을 이용한 병렬(Concurrent) 통신으로 수정하여 로딩 속도 획기적으로 개선
- [복구 완료] 차트 테스트를 위해 임시로 해제했던 과거/미래 날짜 이동 방지 로직(화살표 버튼 및 캘린더 `minDate`, `maxDate` 제한)을 다시 서비스 개시일 및 오늘 날짜 기준 정상 로직으로 원상복구함
- 데이터 패칭 구조 리팩토링: SvelteKit `load` 함수(`+page.ts`)에서 모든 API를 호출하던 방식을 제거하고, 클라이언트 컴포넌트(`+page.svelte`) 내에서 `$effect`를 사용해 메인 차트와 하단 표가 각자의 날짜 변경 시 독립적으로 데이터를 갱신하도록 분리하여 불필요한 전체 렌더링 및 API 중복 호출 문제 해결
- 버그 픽스 (의존성): Svelte 5 `$effect` 내부에서 `data.space_id`를 직접 참조하여 `goto` 발생 시 `data` 객체 갱신으로 인해 양쪽 패칭 로직이 모두 재실행되던 의존성(Reactivity) 문제를 `$derived` 분리로 해결
- 버그 픽스 (차트 애니메이션): 날짜 변경 시 차트에 바인딩된 `selectedDate`가 즉시 갱신되며 구 데이터로 차트가 재그려지는 '이중 렌더링' 현상을 해결하기 위해, 차트 전용 기준 날짜(`chartDisplayedDate`) 상태를 분리하고 API 로딩 완료 시점에만 동기화하도록 수정


- 버그 픽스 (보안 및 에러 핸들링): URL 파라미터 조작을 통한 비정상적인 API 요청(Parameter Injection 및 `NaN` 에러)을 방지하기 위해 `+page.ts`에 정규식(`^\d{4}-\d{2}-\d{2}$`) 검증을 추가하고, API 호출 시 `encodeURIComponent` 처리를 적용함.
- 버그 픽스 (무한 로딩 UX 개선): 네트워크 오류 등 통신 실패 시 `Promise.all`에서 반환된 `null`을 감지하여 상태를 갱신하지 않고 "로딩 중..." 문구가 무한히 노출되던 문제를 수정, 실패 시 "데이터 불러오기 실패" 등 명확한 에러 상태로 처리되도록 대응 로직 추가.
- 버그 픽스 (Svelte 5 반응성 패턴 개선): 불필요한 `$effect` 내 `$state` 동기화 로직을 제거하고 `$derived(data.targetDate)`로 단일화하여 렌더링 최적화 및 상태 꼬임 방지.
- 버그 픽스 (타입스크립트): `SpaceStatus` 인터페이스 스펙(`src/lib/types.ts`)에 맞지 않게 존재하지 않는 `count`, `result` 필드로 초기화하던 오류를 수정하고 필수 필드인 `congestion_level`을 `0`으로 명시하여 컴파일 에러 및 렌더링 시점의 `NaN` 문제를 해결함.
- 버그 픽스 (상태 누수 방지): 사용자가 다른 공간이나 날짜로 이동할 때, 새로운 API 요청이 완료되기 전까지 이전 데이터(잔상)가 화면에 남아 잘못된 정보를 제공할 수 있는 위험(State Leakage)을 방지하기 위해 `$effect` 내부에서 페칭 직전에 상태를 동기적으로 초기화하도록 수정함.
- 버그 픽스 (UX 개선 및 렌더링 루프 방지): 동일한 공간(space_id) 내에서 날짜만 변경했을 때 제목이 깜빡이는 현상을 방지하고자 이전 `space_name`을 읽어오는 로직을 추가했으나, `$effect` 내부에서 상태(`status`)를 읽고 쓰는 과정이 무한 렌더링 루프(Infinite Loop)를 유발함. 이를 해결하기 위해 Svelte 5의 `untrack`을 도입하여 의존성 추적을 안전하게 끊어내고 버그를 완벽히 픽스함.
- 버그 픽스 (API 최적화 및 UX 개선): 날짜(`selectedDate`) 변경 시, 날짜와 무관한 "실시간 혼잡도(`status`)"까지 불필요하게 0%로 초기화되었다가 재요청되면서 뱃지가 깜빡이는 현상(예: 11% -> 0% -> 11%)을 해결하기 위해, `status` 페칭과 `history` 페칭 로직을 두 개의 독립된 `$effect`로 분리하여 날짜 변경 시에는 차트 데이터만 갱신되도록 구조를 최적화함.
- UI 개선 (혼란 방지): '오늘(today)' 날짜를 조회할 때만 제목 옆에 '실시간 혼잡도 퍼센트 뱃지'를 렌더링하고, 과거 등 다른 날짜를 조회할 때는 해당 뱃지를 숨겨서(Svelte `{#if}`) 사용자가 과거 데이터와 실시간 데이터를 혼동하지 않도록 기획 의도에 맞게 직관성을 높임.
- 버그 픽스 (날짜 파라미터 간섭): 페이지 최초 진입 후 하단 표 날짜를 조작하지 않은 상태(URL 파라미터 없음)에서 메인 차트 날짜만 변경할 경우, `+page.ts`의 기본값 폴백(`peakDateStr = targetDate`) 로직으로 인해 하단 표 날짜가 메인 차트 날짜를 강제로 따라가는 현상을 수정함. 차트 날짜 변경 시 URL에 `peak_target_date`가 없다면 현재 표 날짜를 명시적으로 주입하도록 조치.
- UX 개선 (시각적 깜빡임 방지): 날짜 변경 시 이전 데이터를 강제로 비우고(`null` 처리) 다시 렌더링하는 과정에서 전체 차트와 표가 깜빡이며 사라졌다 나타나는 문제를 해결함. 공간(`space_id`)이 완전히 바뀌는 경우에만 데이터를 비우고, 날짜만 변경할 때는 기존 데이터 화면 위에 투명한 로딩 스피너(오버레이)만 띄워둔 채 부드럽게 새 데이터로 전환되도록 Svelte 상태 초기화 로직을 개선함.
- 보안 및 버그 픽스 (비정상적 날짜 접근 차단): UI 상의 달력 버튼을 막아두었음에도 사용자가 직접 URL 파라미터(`?target_date=...`)를 조작하거나 네이티브 모바일 날짜 선택기를 악용해 서비스 개시일(`2026-06-04`) 이전 혹은 미래 날짜로 강제 이동할 수 있었던 보안 취약점을 해결함. `+page.ts` (서버사이드 데이터 로드) 단에서 URL의 날짜를 가로채어 유효 범위를 벗어날 경우 강제로 교정(Clamping)하도록 완벽한 방어벽을 구축함.
- 버그 픽스 (UI 버튼 비활성화 누락 및 URI 인코딩): 이전 코드 수정 과정에서 누락되었던 메인 차트 및 하단 표의 '이전/다음' 날짜 이동 버튼 비활성화(`isNextDisabled`, `isPrevDisabled`) 로직을 재적용하여 UI 상에서 비정상적인 클릭을 원천 차단함. 또한, 모든 API 페칭 로직(`/status`, `/history`, `/peaks`)의 URL에 삽입되는 동적 경로 파라미터(`space_id`)에 `encodeURIComponent` 처리를 일괄 적용하여 잠재적인 Path Injection 취약점을 제거함.

### 예정 사항
- [x] 모바일 달력 시각적 비활성화(flatpickr) 및 반응형 UI 점검
- [x] 헤더 레이아웃 및 폰트 사이즈 최적화
- [x] [임시 변경 복구] 차트 테스트를 위해 임시로 해제했던 과거 날짜 접근 제한(2020년 1월 1일)을 다시 정상 로직(`todayStr`, `yesterdayStr`)으로 복구하여 과거 데이터 조회를 재차단함.
