# 프로젝트 메모리 (AsangP Frontend)

## 2026-05-11 월요일
### 진행 상황
- SvelteKit 2.0 및 Svelte 5 (TypeScript) 프로젝트 초기화 완료
- Prettier, ESLint 추가 완료
- `MEMORY.md` 생성 및 초기 기록 시작
- 백엔드 API 연결 완료 (`https://api.eulhane.com/asangp`)
- `.env` 파일을 통한 API 베이스 URL 관리 설정 (서버 주소 반영)
- 의존성 패키지 설치 완료 (`npm install`)
- CORS 이슈 해결을 위한 Vite 프록시(Proxy) 설정 적용 (`/api` 경로)
- 메인 대시보드 구현 완료 (4개 공간 실시간 상태 표시 및 상세 페이지 연결)

## 2026-05-26 화요일
### 진행 상황
- 상세 페이지에 Chart.js 연동 및 7일 전 대비 혼잡도 추세 차트 구현
- Chart.js `time` 스케일 및 `date-fns` 어댑터 도입으로 시계열 줌/팬 오류 근본 해결
- X축 레이블 10분 단위 정밀 정렬 (`ticks.source: 'data'`)
- UX 개선: 혼잡도 숫자 숨김 처리 및 상태 텍스트(여유/보통/혼잡) 툴팁 적용
- 날짜 선택 기능 추가 (과거 데이터 조회 지원)
- 대시보드 레이아웃 개선: 상단 바 파란색 배경 적용 및 카드 레이아웃 유연화 (고정 비율 제거 및 가로 확장 지원)

## 2026-06-01 월요일
### 진행 상황
- 상세 페이지 하단 피크 시간대 표 3열에 24시간 추세 미니 차트(스파크라인) 개선 및 적용
- 스파크라인 차트의 X축/Y축, 툴팁, 범례 숨김 처리
- 스파크라인 내 줌(Zoom) 및 팬(Pan) 기능 명시적으로 비활성화 및 애니메이션 제거하여 성능 최적화
- 데이터 중 `null` 값 처리 오류를 수정하여 빈 데이터를 0으로 간주하도록 수정 (`val ?? 0` 적용)
- 스파크라인 차트의 선 굵기를 1px로 얇게 통일하고, 0일 때 캔버스 경계에 선이 잘려 얇아 보이는 시각적 이질감을 방지하기 위해 상하 여백(Padding) 추가
- 메인 페이지에 있던 전역 CSS(글꼴, 스크롤 방지)가 라우팅 시 다른 페이지에 영향을 주어 발생하던 스크롤 불가 및 폰트 미적용 버그 해결 (`app.css` 전역 분리 적용)
- 메인 페이지의 상단 로고 및 타이틀 헤더를 `Header.svelte` 컴포넌트로 분리하고 상세 페이지에도 동일하게 적용
- 전체 앱의 기본 배경색을 `#f5f7fa`로 통일하여 일관성 개선
- 상세 페이지의 불필요한 기존 헤더 블록 삭제 및 PC 화면(`1024px` 이상)에서 스크롤 없이(One-page) 한눈에 볼 수 있도록 차트와 표를 좌우 분할 그리드 레이아웃으로 개편
- 우측 피크 시간대 표의 날짜 형식을 간결하게 변경('2026-05-25' -> '5월 25일')하고 공간 확보를 위해 테이블 헤더(th) 행 삭제
- 상세 페이지 그래프 범례(Legend) 숨김 처리하여 불필요한 텍스트 제거
- 차트 내 대상일(오늘) 혼잡도 선 색상을 헤더와 동일한 짙은 남색(`#072e5d`)으로 변경하고, 7일 전 비교선은 회색 실선으로 변경 및 두 선의 굵기를 1px로 얇게 조정하여 디자인 통일감 부여
- 화면 크기 축소 시 차트와 표가 그리드 밖으로 잘리는 현상(Overflow)을 방지하기 위해 각 섹션에 `min-width: 0` 속성을 추가하여 반응형 레이아웃 정상화
- 모바일(1024px 미만) 환경에서는 차트와 표가 세로로 배치되는 점을 고려해 화면 높이 고정(`100vh`)을 해제하고 자연스러운 네이티브 세로 스크롤이 가능하도록 모바일 최적화 수정
- 상세 페이지의 차트 및 표 컨테이너 디자인을 매트(Matte)하고 깔끔한 무드로 변경(반투명 블러 효과, 그림자, 테두리 제거 후 단색 흰색 배경 적용)
- 우측 피크 시간대 표의 스크롤바를 완전히 제거하고, 표 크기(`height: 100%`)가 자동으로 부모 컨테이너 높이에 딱 맞게 늘어나 한눈에 전체 데이터가 꽉 차게 보이도록 수정

### 예정 사항
- API 응답 예외 처리 및 로딩 상태 시각화 강화
- 전체적인 디자인 및 모바일 반응형 디테일 점검
